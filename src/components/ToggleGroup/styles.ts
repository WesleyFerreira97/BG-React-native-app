import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    toggleGroup: {
        flex: 1,
    }
});

// Ambos campos ( letter e number ) são registrados no formulário
// o campo type product vai ser responsável por analizar quais informações vão para o banco de dados 